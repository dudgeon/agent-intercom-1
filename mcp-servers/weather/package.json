{
  "name": "@agent-intercom/weather-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Weather capability MCP server + ui:// MCP App (ADR 0005). Workers-compatible; ambient timed-persistence card (Q6).",
  "main": "src/server.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
