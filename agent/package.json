{
  "name": "agent-intercom-backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Hosted backend: Worker + Session Durable Object (gateway+harness) per ADR 0005/0006.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler deploy --dry-run --outdir=.dryrun",
    "verify": "tsx scripts/verify-backend.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.95.0"
  }
}
